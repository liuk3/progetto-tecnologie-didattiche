version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: testo-giallo-web
    env_file:
      - .env
    ports:
      - "80:80"
    restart: unless-stopped
